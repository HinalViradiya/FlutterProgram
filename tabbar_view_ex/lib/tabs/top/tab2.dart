import 'package:flutter/cupertino.dart';

class Tab2 extends StatelessWidget {
  const Tab2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Container(
      child:const Center(child: Text("Tab2",style: TextStyle(fontSize: 24),))
    );
  }
}